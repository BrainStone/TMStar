plugins {
    id 'cpp-application'
    id 'visual-studio'
}

application {
    targetMachines = [
        machines.windows.x86_64
    ]
}

tasks.withType(CppCompile).configureEach {
    compilerArgs.addAll '/std:c++17', '/EHsc'
}
